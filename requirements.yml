---
# Root requirements.yml — install all collections needed to run the demo.
# Usage:  ansible-galaxy collection install -r requirements.yml
collections:
  - name: ansible.posix
    version: ">=1.5.4"
  - name: middleware_automation.common
    version: ">=1.2.0"
  - name: middleware_automation.wildfly
    version: ">=1.5.14"
